# Given an array of integers, return the greatest three.

def find_greates_three(n):

    hash_table = {}
    for el in n:
        if el not in hash_table:
            hash_table[el] = True
        else:
            hash_table[el] = False


    for 
